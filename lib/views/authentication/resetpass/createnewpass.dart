import 'package:flutter/material.dart';

class Createnewpass extends StatelessWidget {
const Createnewpass({ Key? key, required email, required resetToken }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}